todo:
	use ewmh lib to fetch x11 info
done:	
	use configparser to manage config
	use subprocess to excute cmd (subprocess.check_output(cmd,shell=True).decode("utf-8").strip())
	bar font suppurt english,chinese,japanese,korean,cyrillic,othor font(like nerd fonts)(ai help a lot lol)
	use dpg to make bar:
		size,font color(dpg.mvThemeCol_Text),background color(dpg.mvThemeCol_WindowBg),style(dpg.mvStyleVar_WindowPadding)
